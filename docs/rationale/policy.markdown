# Rationale: Policy

Read when: auditing, challenging, or replacing the formatter policy predicates, policy defaults, or policy-key domains described in [`../design/features/policy.markdown`](../design/features/policy.markdown).

Defines: non-normative evidence, assumptions, gaps, and tradeoff reasoning behind user-facing policy choices, bare-display interpretation, default policy behavior, and the keycap-character policy domain.

Does not define: current policy behavior, recognition behavior, or slot-classification behavior. For behavior, see [`../design/features/policy.markdown`](../design/features/policy.markdown), code, tests, and public API documentation. For recognition and classification rationale, see [`recognition.markdown`](recognition.markdown) and [`classification.markdown`](classification.markdown).

This file follows the rationale authoring and manual review conventions in [`authoring.markdown`](authoring.markdown).

## Inferences

### Bare Display Assumption

Manually reviewed: yes.

Product assumption: **when a selector slot is represented by a policy key and both bare and explicit selector spellings are reasonable, bare rendering aligns with either the text-selector rendering or the emoji-selector rendering.** `evfmt` treats that as a display assumption only. It does not decide whether bare spelling is canonical, and it does not apply to fixed-cleanup cases that resolve before policy.

This assumption lets `bare_as_text` be a binary presentation-side predicate: bare spelling is treated as text presentation when the policy key is in `bare_as_text`, and as emoji presentation otherwise. That is the same kind of policy-model tie-breaker as the spelling-shape choice below; it is not a claim that Unicode presentation semantics outside `evfmt` are only binary.

Evidence gap: renderers, fonts, editors, terminals, or platforms may display the bare spelling for a policy key in a way that is meaningfully distinct from both explicit selector forms. Evidence of that behavior for important environments should trigger a revisit of the affected default policy membership or policy model.

### Final Selector Tie-Breaker

Manually reviewed: yes.

Facts: policy is consulted only after classification has left more than one reasonable selector state. At that point, `evfmt` still formats toward one canonical representative instead of preserving whichever reasonable spelling appeared in the input.

Product assumption: **when bare spelling is not preferred for a policy key, the explicit selector for the selected presentation side is the canonical spelling.** This is why the public predicate is `prefer_bare`: within this canonical policy model, `prefer_bare = false` means bare is not the canonical spelling, so the selected side is written explicitly. A hypothetical negative `prefer_selector` flag would not express the same model, because `prefer_selector = false` would not by itself choose bare as canonical.

This tie-breaker mainly rejects the no-op alternative, where every reasonable spelling is left unchanged. A canonical policy must choose whether bare or explicit spelling wins for the key; it cannot leave both as equally canonical. That choice applies only within the active policy setting: if there is an important reason to prefer bare for a key, the default policy or the user's policy should put that key in `prefer_bare`; otherwise, explicit spelling is the final tie-breaker.

### Small Policy Context Surface

Manually reviewed: yes.

Facts: after local selector context classification and single-state cleanup, the current policy surface uses two domains indexed by variation-sequence base character: non-keycap policy keys and keycap-character policy keys.

Product assumption: **the public policy context surface should expose only durable, named distinctions that users can reason about.** "Small" does not mean a fixed count. It means policy domains should not be derived from arbitrary surrounding sequence topology.

Domain-qualified base-indexing is the current public realization of that constraint, not the fundamental principle. The current non-keycap/keycap domains keep policy compact while preserving the keycap distinction argued by the keycap-specific rationale below.

Revisit this policy-shape choice if future Unicode data introduces policy-relevant selector contexts that do not fit the current domains, or if real user needs show that the current non-keycap/keycap policy surface cannot express an important formatting choice. The response should still expose a named policy distinction that users can reason about, such as adding a small domain or replacing the current domain split with another small policy model, rather than encoding arbitrary surrounding sequence topology into policy keys.

### Named Policy-Set Surface

Manually reviewed: yes.

Facts: the 0.3.0 policy-key namespace omits the earlier convenience subsets `rights-marks`, `arrows`, and `card-suits`. Those subsets let users keep a small class of text-default characters bare without selecting all `text-defaults`. The same policies remain expressible with singleton characters or code points, but may require longer option values. At the time of this namespace decision, no known external workflow depended on the convenience names.

Product assumption: **the initial named-set surface should omit these convenience subsets until a concrete workflow shows that the remaining named sets and short singleton lists are too cumbersome.** A small surface is easier to explain and leaves room to add a set later without breaking existing policy expressions. Namespace size alone does not decide the issue: a common policy that repeatedly requires long singleton lists would justify reconsidering a focused convenience name.

Revisit this choice when real configurations show that `text-defaults`, `emoji-defaults`, `variation-bases`, `keycap:rgi`, and short singleton lists do not express a common policy clearly.

### Why There Is No `keycap:ascii`

Manually reviewed: yes.

Product assumption: **users expect `ascii` to refer to ordinary ASCII characters such as `#`, `*`, and digits, not to their use as keycap bases.** The policy namespace therefore provides `keycap:rgi` and does not provide `keycap:ascii`.

### Keycap-Character Policy Domain

Manually reviewed: yes.

Facts: keycap sequences use a base followed by `U+20E3` COMBINING ENCLOSING KEYCAP, optionally with a presentation selector between them. `U+20E3` predates Unicode emoji, so bare keycap spellings such as `[0-9#*] U+20E3` were possible as Unicode text before modern emoji qualification. Historical Unicode source mappings give direct carrier-set evidence for bare `#` and digit keycap spellings: they round-trip to DoCoMo, KDDI, and SoftBank Shift-JIS carrier symbols. Older emoji data also recorded bare keycap spellings, and later named-sequence records document the addition of `FE0F` to keycap spellings. Modern emoji qualification uses `[0-9#*] FE0F U+20E3` for fully qualified emoji keycaps. The pinned `emoji-test.txt` records the corresponding bare forms as unqualified.

Sources: [UTS #51](https://www.unicode.org/reports/tr51/) defines emoji keycap sequences as `[0-9#*] FE0F U+20E3`; the pinned `emoji-sequences.txt` lists those forms as `Emoji_Keycap_Sequence`, and the pinned `emoji-test.txt` lists the corresponding bare forms as unqualified. The pinned `emoji-variation-sequences.txt` lists `FE0E` and `FE0F` variation sequences for `#`, `*`, and `0`-`9`. [`EmojiSources.txt`](https://www.unicode.org/Public/UCD/latest/ucd/EmojiSources.txt) provides historical round-trip mappings between bare `#` and digit keycap sequences and DoCoMo, KDDI, and SoftBank Shift-JIS carrier symbols. Unicode Emoji 1.0 [`emoji-data.txt`](https://www.unicode.org/Public/emoji/1.0/emoji-data.txt) recorded bare keycap sequences with source data, Unicode Emoji 2.0 [`emoji-sequences.txt`](https://www.unicode.org/Public/emoji/2.0/emoji-sequences.txt) listed bare keycap sequences directly, and [`NamedSequences.txt`](https://www.unicode.org/Public/17.0.0/ucd/NamedSequences.txt) records that `FE0F` was added to the named keycap sequences in 2016. [Unicode Standard Chapter 23](https://www.unicode.org/versions/latest/core-spec/chapter-23/) and [ISO/IEC 10646 PDAM 2](https://www.unicode.org/L2/L2012/12199-02n4228_10646pdam2.pdf) discuss variation-selector keycap spellings and the effect of `FE0F` before `U+20E3`.

Inference: **keycap-character selector slots need a separate policy-key domain because the independently justified defaults for ordinary ASCII slots and keycap slots require different canonical spelling choices for the same base characters.** Ordinary `#`, `*`, and digit slots should prefer bare spelling: these characters are common source text, and the default formatter should not introduce invisible selectors into ordinary ASCII. Bare keycap sequences, however, are structured `base U+20E3` spellings with Unicode-text legitimacy, historical carrier-compatibility evidence for most RGI keycap bases, and emoji-data ambiguity. When the default policy treats them as text-side rather than emoji intent, the canonical text-side spelling should be explicit `FE0E` before `U+20E3`, not the ambiguous bare keycap spelling. A base-character-only policy could not express both defaults for `#`, `*`, and digits. The keycap-character domain is therefore needed to keep the ordinary ASCII bare default and the keycap text-side default simultaneously expressible.

Evidence gap: the data and history support a distinct domain, but they do not prove that this is the simplest maintainable policy structure. The historical carrier mappings prove equivalent carrier symbols for bare `#` and digit keycaps, but they do not show usage frequency, user intent, or how often bare keycap spellings appeared in archived Unicode text after transcoding.

### Bare Default for Bases with RGI Emoji Keycap Sequences

Manually reviewed: yes.

Facts: bare keycap inputs such as `# 20E3` are present in Unicode emoji data as unqualified forms, while the corresponding RGI emoji keycap sequences use `base FE0F U+20E3`. The bases with RGI emoji keycap sequences are `[0-9#*]`; they are text-default characters and have both text and emoji variation-sequence data in the pinned Unicode data.

Product assumption: **the default formatter behavior treats a bare keycap-character form for a base with an RGI emoji keycap sequence as text-style source unless policy says otherwise.** These bases are text-default characters, so a bare spelling follows their current Unicode default. The fact that Unicode emoji data also lists the bare keycap form as unqualified emoji data describes its emoji qualification status; it does not by itself transfer the text-default bare spelling into emoji intent. This favors preserving an observed text-default base spelling over silently promoting it to fully qualified emoji.

Intent ambiguity is the weak point for contemporary bare keycap inputs. Bare keycap spellings are well-formed Unicode text, and their bases are text-default characters whose corresponding RGI emoji sequences use `base FE0F U+20E3`. Historical Unicode source mappings also round-trip bare `#` and digit keycaps to Japanese carrier symbols, Unicode Emoji 1.0 recorded bare keycaps with source data, and Unicode Emoji 2.0 listed bare keycaps directly as emoji sequences. AOSP LatinIME in Android 17 still inserts bare keycaps from its emoji keyboard. Under the default policy, `evfmt` interprets that emoji-keyboard output as text-style input. The same bare scalar sequence can therefore carry text or emoji intent. A formatter that receives only the Unicode text must choose a tie-breaker and will disagree with some producers.

The current Unicode default presentation remains a defensible tie-breaker for this unavoidable ambiguity. Evidence from common tools showing that bare keycaps are overwhelmingly produced or perceived as emoji would weaken this default; evidence that users rely on text-styled enclosed keycaps would strengthen it. The implementation survey below records the available evidence about producer behavior and whether the resulting text can plausibly reach `evfmt`.

#### Contemporary Implementation Evidence

Implementation evidence checked through 2026-07-26:

The relevance labels ask whether the observed text could plausibly become `evfmt` input. Strong evidence follows a version-matched path into a general-purpose active editor or clipboard text intended for transfer. Weak evidence covers application-specific authored text, downstream text, or an incomplete producer path whose output would reach `evfmt` only occasionally or remains partly inferred. Neutral evidence produces authored text in a third representation without choosing either Unicode keycap spelling. “None yet” marks internal data or an unresolved lead with no demonstrated path to text that `evfmt` would encounter. These labels measure the completeness and relevance of each output path, not its market share or independence from another implementation lineage.

| Implementation                                                                                 | Keycap behavior                                                                                   | Relevance to plausible `evfmt` input                                                                            |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| GitHub Markdown API, version `2026-03-10`                                                      | Shortcodes expand to fully qualified keycaps in rendered HTML                                     | Weak: downstream rendering output, not authored source                                                          |
| AOSP LatinIME, Android 7–17 release-1 tags                                                     | Commits bare keycaps into the active text field                                                   | Strong: complete source path to `commitText` in every checked tag; upstream alone does not establish deployment |
| GrapheneOS, Android 17 branch                                                                  | Ships an AOSP-derived system keyboard that commits bare keycaps                                   | Strong: shipped system keyboard with a complete source path; primary-keyboard usage is unknown                  |
| LineageOS 23.2 LatinIME fork                                                                   | Ships fully qualified keycaps and commits them into the active text field                         | Strong: shipped system keyboard with a complete source path                                                     |
| Gboard `17.7.7.932364120`                                                                      | Tap output unresolved; shipped data contains only fully qualified candidates                      | None yet: packaged candidates have no path to `InputConnection`                                                 |
| SwiftKey `9.13.11.5` for Android, symbols panel                                                | Candidate selection prefers fully qualified, then bare, and omits the item if neither has a glyph | Weak: selected candidate has not been traced to `InputConnection`                                               |
| iOS 8.4 and 9.0 simulator and iOS 9.0.2 physical                                               | Stored category values for `#` and digit keycaps are bare; `*` is absent                          | Weak: category values lack a version-matched insertion path                                                     |
| Available iOS simulator dumps from 9.1 through 13.2.2 and physical dumps from 9.3 through 13.2 | Stored category values for all 12 keycaps are fully qualified                                     | Weak: category values lack a version-matched insertion path                                                     |
| iOS/iPadOS 26.5.2                                                                              | Tap output unresolved                                                                             | None yet: iOS 26.1 preserves category strings, but current values are inaccessible                              |
| OS X 10.7.5–10.8.5                                                                             | Stored category values for `#` and digit keycaps are bare; `*` is absent                          | Weak: category values lack a version-matched insertion path                                                     |
| OS X 10.9.5                                                                                    | Stored digits are fully qualified, `#` is bare, and `*` is absent                                 | Weak: category values lack a version-matched insertion path                                                     |
| OS X 10.10.2–10.10.3                                                                           | Stored digits and `#` are fully qualified; `*` is absent                                          | Weak: category values lack a version-matched insertion path                                                     |
| macOS 26.5.2                                                                                   | Tap output unresolved                                                                             | None yet: current category values and keycap capture are unavailable                                            |
| KDE Plasma emoji selector                                                                      | Copies fully qualified keycaps to the clipboard                                                   | Strong: clipboard text can be pasted directly into source                                                       |
| Mattermost 11.10.0                                                                             | Inserts fully qualified keycaps into the composer                                                 | Weak: literal Unicode enters an application-specific editor that rarely feeds `evfmt`                           |
| Element Web 1.12.24                                                                            | Inserts fully qualified keycaps into the composer                                                 | Weak: literal Unicode enters an application-specific editor that rarely feeds `evfmt`                           |
| Mastodon 4.7.0-alpha.1                                                                         | Inserts fully qualified keycaps into compose state                                                | Weak: literal Unicode enters application-specific authored text that rarely feeds `evfmt`                       |
| Zulip message composer                                                                         | Inserts ASCII shortcodes                                                                          | Neutral: authored text contains no Unicode keycap spelling                                                      |
| Zulip 12.1 message notifications                                                               | Materializes message shortcodes as bare keycaps in notification text                              | Weak: downstream notification text rarely feeds `evfmt`                                                         |
| Discourse message composer                                                                     | Inserts ASCII shortcodes                                                                          | Neutral: authored text contains no Unicode keycap spelling                                                      |
| Emojipedia                                                                                     | Copies a fully qualified sampled keycap                                                           | Strong: clipboard payload can be pasted directly into source; no GUI round trip was performed                   |
| Emojiguide                                                                                     | Copies a fully qualified sampled keycap                                                           | Strong: clipboard payload can be pasted directly into source; no GUI round trip was performed                   |
| Get Emoji                                                                                      | Copies a fully qualified sampled keycap                                                           | Strong: clipboard payload can be pasted directly into source; no GUI round trip was performed                   |
| EmojiMeaning                                                                                   | Copies a fully qualified sampled keycap                                                           | Strong: clipboard payload can be pasted directly into source; no GUI round trip was performed                   |
| iEmoji                                                                                         | Copies a fully qualified sampled keycap                                                           | Strong: clipboard payload can be pasted directly into source; no GUI round trip was performed                   |
| OmgSymbol                                                                                      | Copies a fully qualified sampled keycap                                                           | Strong: clipboard payload can be pasted directly into source; no GUI round trip was performed                   |
| Samsung Keyboard 5.9.30.97                                                                     | Tap output unresolved                                                                             | None yet: no scalar evidence                                                                                    |
| Windows 11 picker                                                                              | Tap output unresolved                                                                             | None yet: no keycap scalar evidence                                                                             |

Detailed evidence:

- **GitHub Markdown API.** A live [Markdown API](https://docs.github.com/en/rest/markdown/markdown) request using API version `2026-03-10` expanded `:zero:` through `:nine:`, `:hash:`, and `:asterisk:` to fully qualified keycaps. A control request containing literal bare keycaps preserved them, so shortcode mapping produced the result. The Unicode appears in rendered HTML rather than editable source; it is only weak evidence about text likely to reach `evfmt`.
- **AOSP LatinIME.** The release-1 tags for every Android major version from 7 through 17 contain all 12 bare keycaps and the complete resource-to-`commitText` path. In Android 17, the [`emoji-categories.xml`](https://android.googlesource.com/platform/packages/inputmethods/LatinIME/+/refs/tags/android-17.0.0_r1/java/res/values-v19/emoji-categories.xml#1279) strings pass through [`CodesArrayParser`](https://android.googlesource.com/platform/packages/inputmethods/LatinIME/+/afe0d5c261c3a0c88b38894981a7b332da93e7dd/java/src/com/android/inputmethod/keyboard/internal/CodesArrayParser.java#95), [`KeyboardBuilder`](https://android.googlesource.com/platform/packages/inputmethods/LatinIME/+/afe0d5c261c3a0c88b38894981a7b332da93e7dd/java/src/com/android/inputmethod/keyboard/internal/KeyboardBuilder.java#433), [`EmojiPalettesView`](https://android.googlesource.com/platform/packages/inputmethods/LatinIME/+/afe0d5c261c3a0c88b38894981a7b332da93e7dd/java/src/com/android/inputmethod/keyboard/emoji/EmojiPalettesView.java#346), and [`InputLogic`](https://android.googlesource.com/platform/packages/inputmethods/LatinIME/+/afe0d5c261c3a0c88b38894981a7b332da93e7dd/java/src/com/android/inputmethod/latin/inputlogic/InputLogic.java#234) to `commitText`; the same five stages were checked in each earlier release-1 tag. Android 4.4–6 resources contain bare `#` and digit output behind legacy PUA labels with `minSupportSdkVersion=99`, which does not establish visible keys. The [December 2015 Unicode 8 update](https://android.googlesource.com/platform/packages/inputmethods/LatinIME/+/e044d12b893caa1647d30b13f6397fcaa31f59d2) introduced the visible all-12 list.
- **GrapheneOS system keyboard.** The Android 17 product build [includes `LatinIME`](https://github.com/GrapheneOS/platform_build/blob/7f0398241bc8c4ef5255a8063befa5045e5cfab4/target/product/handheld_product.mk#L24-L36). Its current [`emoji-categories.xml`](https://github.com/GrapheneOS/platform_packages_inputmethods_LatinIME/blob/b27001115af9768088064de94a605ecdb6b8aa85/java/res/values-v19/emoji-categories.xml#L1279-L1290) contains all 12 bare keycaps. Selection passes the output string through [`onTextInput`](https://github.com/GrapheneOS/platform_packages_inputmethods_LatinIME/blob/b27001115af9768088064de94a605ecdb6b8aa85/java/src/com/android/inputmethod/keyboard/emoji/EmojiPalettesView.java#L338-L355) and then to [`commitText`](https://github.com/GrapheneOS/platform_packages_inputmethods_LatinIME/blob/b27001115af9768088064de94a605ecdb6b8aa85/java/src/com/android/inputmethod/latin/inputlogic/InputLogic.java#L231-L258). GrapheneOS also [re-enables the system keyboard package at boot and selects it in Safe Mode](https://grapheneos.org/releases#2026010800), so this is a deployed producer and a system fallback even when a user normally selects another keyboard. The fraction of GrapheneOS users who use it as their primary keyboard is unknown.
- **LineageOS LatinIME fork.** The LineageOS 23.2 mobile product [includes `LatinIME`](https://github.com/LineageOS/android_vendor_lineage/blob/e024cea5081666b22da966d51ae966a288a6a27b/config/common_mobile.mk#L16-L22). Its current [`emoji-categories.xml`](https://github.com/LineageOS/android_packages_inputmethods_LatinIME/blob/9d4da58db750f2fb62449849920545dadb08a47b/java/res/values-v19/emoji-categories.xml#L1329-L1341) contains all 12 fully qualified keycaps. Selection sends the output string through [`onTextInput`](https://github.com/LineageOS/android_packages_inputmethods_LatinIME/blob/9d4da58db750f2fb62449849920545dadb08a47b/java/src/com/android/inputmethod/keyboard/emoji/EmojiPalettesView.java#L342-L355) and then to [`commitText`](https://github.com/LineageOS/android_packages_inputmethods_LatinIME/blob/9d4da58db750f2fb62449849920545dadb08a47b/java/src/com/android/inputmethod/latin/inputlogic/InputLogic.java#L239-L263). This fork demonstrates that a currently shipped AOSP-derived keyboard can retain the LatinIME insertion path while changing keycap output from bare to fully qualified.
- **Gboard.** Version [`17.7.7.932364120-release-arm64-v8a`](https://apkpure.net/gboard-the-google-keyboard-app/com.google.android.inputmethod.latin/download/17.7.7.932364120-release-arm64-v8a) has APK SHA-256 `549680c31a0adcdc324fa7b8ab2eeacc108a312e9f2fef1d956f5fc2d56c3ff9`. Its `en_us` binary inside `assets/emoji_en_us_20250115185814.zip` has SHA-256 `32f5d24a37cad593ac86f8d4d9f5cb24d546162938f9e6f38f8160081554cf85`, contains all 12 fully qualified keycap candidates, and contains no bare alternatives. Packaged candidates may serve picker search, display, or insertion; no evidence connects them to the received `InputConnection` string, so they do not establish `evfmt` input.
- **SwiftKey for Android.** Version [`9.13.11.5`](https://apkpure.net/microsoft-swiftkey-keyboard/com.touchtype.swiftkey/download/9.13.11.5) has XAPK SHA-256 `a501a47e7d6d7da8ab61074252d67bbb277be8c68d3e994710d95d0b9a7d4e78` and base-APK SHA-256 `e3fc95caa28c6a79948b20a08c3bead727c1c4e6d6f132ff027d0fde3b62613e`. Its symbols-panel path tests each fully qualified keycap and then its bare alternative with Android [`TextPaint.hasGlyph`](https://developer.android.com/reference/android/graphics/Paint), selects the first supported candidate, and omits the item if neither candidate has a glyph. The selected candidate has not been traced to `InputConnection`, so this remains weak evidence rather than a claim about inserted text. Recents, search, prediction, and migrated persisted data may use other paths.
- **iOS system keyboard.** Extracted `UIKeyboardEmoji.emojiString` category values contain bare keycaps in the [iOS 8.4 simulator data](https://github.com/liuyuning/DumpEmoji/blob/0698fbc8f1d12ef5196cfd69a5843a487a7e6df6/Emojis/Emoji_iOS8.4_Simulator_EmojisInCate_895.plist#L529-L540), [iOS 9.0 simulator data](https://github.com/liuyuning/DumpEmoji/blob/0698fbc8f1d12ef5196cfd69a5843a487a7e6df6/Emojis/Emoji_iOS9.0_Simulator_EmojisInCate_1100.plist#L779-L791), and [iOS 9.0.2 physical-iPhone data](https://github.com/liuyuning/DumpEmoji/blob/0698fbc8f1d12ef5196cfd69a5843a487a7e6df6/Emojis/iPhone/Emoji_iOS9.0.2_iPhone_EmojisInCate_1100.plist#L779-L791), then fully qualified keycaps in the [iOS 9.1 simulator data](https://github.com/liuyuning/DumpEmoji/blob/0698fbc8f1d12ef5196cfd69a5843a487a7e6df6/Emojis/Emoji_iOS9.1_Simulator_EmojisInCate_1284.plist#L1058-L1069) and [iOS 9.3 physical-iPhone data](https://github.com/liuyuning/DumpEmoji/blob/0698fbc8f1d12ef5196cfd69a5843a487a7e6df6/Emojis/iPhone/Emoji_iOS9.3_iPhone_EmojisInCate_1283.plist#L1057-L1068). Available simulator dumps remain fully qualified through iOS 13.2.2, and available physical-iPhone dumps remain fully qualified through [iOS 13.2](https://github.com/liuyuning/DumpEmoji/blob/0698fbc8f1d12ef5196cfd69a5843a487a7e6df6/Emojis/iPhone/Emoji_iOS13.2_iPhone_EmojisInCate_1719.plist#L1473-L1486). A decompilation from iPhone18,3 running iOS 26.1 build 23B85 traces category members unchanged into [`UIKeyboardEmoji`](https://github.com/EthanArbuckle/iPhone18-3_26.1_23B85_Restore/blob/90aa0cfe59d9682b4265e1354c8b19ec3c7823ab/System/Library/PrivateFrameworks/UIKitCore.framework/UIKitCore/UIKeyboardEmojiCategory.mm#L486-L544), the represented string, and finally [`insertText:`](https://github.com/EthanArbuckle/iPhone18-3_26.1_23B85_Restore/blob/90aa0cfe59d9682b4265e1354c8b19ec3c7823ab/System/Library/PrivateFrameworks/UIKitCore.framework/UIKitCore/UIKeyboardEmojiCollectionInputView.mm#L3337-L3345). The Symbols category array is opaque in that decompilation, and the [iOS/iPadOS 26.5.2](https://support.apple.com/es-la/127594) category values are inaccessible inside encrypted system images. A [2013 simulator extraction](https://gist.github.com/mattt/8185075) independently records older bare `#` and digit values. No version-matched tap capture was found for the historical category data.
- **macOS Character Viewer.** CharacterPalette category data records bare `#` and digits in [OS X 10.7.5 and 10.8.5](https://gist.github.com/morozgrafix/be1bf4bd9cacc65c2626), fully qualified digits with bare `#` in OS X 10.9.5, and fully qualified digits and `#` in OS X 10.10.2 and 10.10.3. These datasets omit the `*` keycap. A Character Viewer integration confirms that selection calls [`NSTextInputClient.insertText`](https://stackoverflow.com/questions/62865658/macos-take-emoji-from-characterpalette-revised) with a string, but does not sample keycaps or identify the OS build. [macOS 26.5.2](https://support.apple.com/en-us/127595) category data, insertion-path evidence, and runtime output remain unverified.
- **KDE Plasma.** Its generator [keeps only fully qualified emoji-test rows](https://github.com/KDE/plasma-desktop/blob/06541b5d8b51b70124798555e0f8fb96a0f1f222/emojier/data/update-emoji-data.py#L67-L71), and its emoji selector passes the resulting literal string to [`QClipboard::setText`](https://github.com/KDE/plasma-desktop/blob/06541b5d8b51b70124798555e0f8fb96a0f1f222/emojier/emojierplugin.cpp#L328-L332).
- **Mattermost.** Version 11.10.0 derives its picker data from `iamcal/emoji-data`, converts the fully qualified [`unified` value to literal Unicode](https://github.com/mattermost/mattermost/blob/25f3a75cb7977e1af675cb1f22b704614ea052b5/webapp/channels/src/components/advanced_text_editor/use_editor_emoji_picker.tsx#L63-L70), and inserts that string into the editor.
- **Element Web.** Version 1.12.24 passes the independent Emojibase dataset's fully qualified [`.unicode` value](https://github.com/element-hq/element-web/blob/7e8daab9eeb42c0b0b0fce0a0f32f23116f09122/apps/web/src/components/views/emojipicker/EmojiPicker.tsx#L308-L310) unchanged into its [composer insertion](https://github.com/element-hq/element-web/blob/7e8daab9eeb42c0b0b0fce0a0f32f23116f09122/apps/web/src/components/views/rooms/MessageComposer.tsx#L394-L400). Lookup-only variation stripping does not alter the inserted string.
- **Mastodon.** Version 4.7.0-alpha.1 derives its picker data from `iamcal/emoji-data`. Its compressed `unified` field becomes the `.native` string [inserted into compose state](https://github.com/mastodon/mastodon/blob/de91f2b7fc34e931cb8fc371cb1b2653f7e06a6b/app/javascript/mastodon/reducers/compose.js#L223-L229), so keycaps enter authored posts fully qualified. The separate bare `non_qualified` field is used only as a [fallback SVG filename](https://github.com/mastodon/mastodon/blob/de91f2b7fc34e931cb8fc371cb1b2653f7e06a6b/lib/tasks/emojis.rake#L45-L48); bare entries in its asset map likewise identify images and do not establish emitted text.
- **Zulip message composer.** Selecting an emoji [constructs and inserts](https://github.com/zulip/zulip/blob/beb204db693b7ae87ac3f375bb55d9e32a0c4cc1/web/src/emoji_picker.ts#L974-L979) an ASCII shortcode such as `:one:` rather than a Unicode keycap.
- **Discourse message composer.** The picker passes an emoji name to a [textarea insertion path](https://github.com/discourse/discourse/blob/3614525859cc34e826d1ab5de854abb9d9aecd40/frontend/discourse/app/lib/textarea-text-manipulation.js#L758-L774) that inserts an ASCII shortcode rather than a Unicode keycap.
- **Zulip reaction identifiers are non-evidence about `evfmt` input.** Zulip 12.1 removes `FE0F` while generating its `iamcal/emoji-data`-derived maps, so `Reaction.emoji_code` uses bare-shaped ASCII identifiers such as `"0031-20e3"`. The [model defines this field as a string that identifies a glyph](https://github.com/zulip/zulip/blob/beb204db693b7ae87ac3f375bb55d9e32a0c4cc1/zerver/models/messages.py#L387-L406), and the web reaction UI uses it in an [image-rendering CSS class](https://github.com/zulip/zulip/blob/beb204db693b7ae87ac3f375bb55d9e32a0c4cc1/web/templates/message_reaction.hbs#L1-L11). [`REMAPPED_EMOJIS`](https://github.com/zulip/zulip/blob/beb204db693b7ae87ac3f375bb55d9e32a0c4cc1/tools/setup/emoji/emoji_setup_utils.py#L8-L33) only selects legacy image-farm symlink targets. These identifiers and asset paths do not place literal Unicode keycaps in text that `evfmt` would encounter.
- **Zulip Flutter reaction display is non-evidence about `evfmt` input.** The client [decodes a reaction identifier](https://github.com/zulip/zulip-flutter/blob/01907d833e3d6bec1bdff70485c769f0478dba94/lib/api/model/model.dart#L1000-L1023) and [renders the resulting Unicode string](https://github.com/zulip/zulip-flutter/blob/01907d833e3d6bec1bdff70485c769f0478dba94/lib/widgets/emoji.dart#L96-L176) in a `Text` widget. This can display a bare keycap, but it provides no editable, copied, or exported text path into `evfmt`.
- **Zulip message notifications are weak evidence.** For a message containing a keycap shortcode, mobile push [converts the rendered emoji code to Unicode](https://github.com/zulip/zulip/blob/beb204db693b7ae87ac3f375bb55d9e32a0c4cc1/zerver/lib/push_notifications.py#L848-L856), and web desktop notifications perform the [same conversion](https://github.com/zulip/zulip/blob/beb204db693b7ae87ac3f375bb55d9e32a0c4cc1/web/src/ui_util.ts#L44-L83). The generated notification text contains a bare keycap. Notification text is a downstream channel that users rarely save or feed to `evfmt`, so this is weak evidence about plausible formatter input rather than authored-source evidence.
- **Emojipedia.** The [keycap page](https://emojipedia.org/keycap-digit-one) exposes a fully qualified copy payload for the sampled keycap.
- **Emojiguide.** The [keycap page](https://emojiguide.com/keycap-1/) exposes a fully qualified copy payload for the sampled keycap.
- **Get Emoji.** The [keycap page](https://www.get-emoji.com/keycap-1/) exposes a fully qualified copy payload for the sampled keycap. Its bare-looking `0031-20e3.png` filename is an image asset key and does not describe the copied text.
- **EmojiMeaning.** The [keycap page](https://www.emojimeaning.com/keycap) exposes a fully qualified copy payload for the sampled keycap.
- **iEmoji.** The [keycap page](https://www.iemoji.com/view/emoji/295/symbols/keycap-1) exposes a fully qualified copy payload for the sampled keycap. Its bare “iOS 5 Code” entry is historical reference content rather than the current Copy button payload.
- **OmgSymbol.** The [keycap page](https://omgsymbol.com/keycap-1/) exposes a fully qualified copy payload for the sampled keycap.
- **Samsung Keyboard.** Version [`5.9.30.97`](https://www.apkmirror.com/apk/samsung-electronics-co-ltd/samsung-honey-board/samsung-keyboard-5-9-30-97-release/) was posted on 2026-07-25 as one current release branch. Its keycap scalar output remains unresolved because the available distribution route does not provide the APK and representative execution requires compatible Samsung firmware and hardware.
- **Windows 11 picker.** General-release builds 26200.8894 for Windows 11 25H2 and 26100.8894 for Windows 11 24H2 were current under the [2026-07-18 update](https://support.microsoft.com/en-us/servicing/os/windows-11/2026/07/kb5121767-out-of-band). [Microsoft's picker documentation](https://support.microsoft.com/en-us/help/17073/windows-using-keyboard) describes inserting an emoji into the active text field without exposing its scalar payload. No public keycap-specific runtime capture was found.

Among current bare-producing implementations, AOSP LatinIME and the GrapheneOS system keyboard form the only strong implementation lineage found in this review. Zulip notification text supplies separate weak evidence. Historical Apple category data supplies older weak evidence. Current strong and weak fully-qualified evidence spans a broader set of independent implementations, and the LineageOS fork shows that a deployed LatinIME derivative has adopted fully qualified output.

Text and emoji intents remain mixed in bare keycap text, and the scalar sequence carries no provenance that would let a deterministic formatter recover every producer's intent. Future convergence on fully qualified emoji keycaps would place explicit `FE0F` on contemporary emoji-intended input more consistently. `evfmt` already preserves recognized exact RGI input, including fully qualified keycaps. Applying Unicode default presentation to the remaining bare ambiguity would then align the formatter's tie-breaker with Unicode's default-presentation and variation-selector model. This is a product inference from the standard's observable distinctions, not a Unicode conformance requirement.

The copy-page inspection traced HTML payloads through each site's copy handler without performing GUI clipboard round trips. Revisit the default if broader runtime evidence shows that contemporary bare keycap input usually carries emoji intent.

Audit note: verify picker behavior from the inserted or copied scalar sequence and ask whether that text can plausibly reach `evfmt`. Record the product and version, follow data through the insertion path when using source evidence, and distinguish shared upstream datasets from independent implementations. Internal identifiers, image filenames, asset keys, CSS classes, and display-only Unicode materialization do not establish text that can reach `evfmt`. Category records, glyph-selection candidates, and other incomplete producer paths provide at most weak contextual evidence until a version-matched path connects them to relevant output. Keep all such findings explicitly labeled as weak context, unresolved leads, or non-evidence.

### Bare Default for Bases Without RGI Emoji Keycap Sequences

Manually reviewed: yes.

Facts: Unicode emoji keycap sequence data defines RGI keycap bases only for `#`, `*`, and `0`-`9`. Other variation-sequence bases followed by `U+20E3` do not have Unicode emoji keycap sequence semantics, even though `evfmt` still treats the local selector slot as a keycap-character policy context.

Product assumption: **the default formatter behavior should not synthesize emoji intent for keycap-character forms whose bases have no RGI emoji keycap sequences.** With no Unicode emoji keycap semantics to preserve, a bare keycap-character form for such a base is treated as text-side source by default. The final selector tie-breaker then makes the explicit text selector canonical when bare spelling is not preferred, so default formatting inserts `FE0E` before `U+20E3`.

### Text Selector Preservation in Keycap-Character Context

Manually reviewed: yes.

Facts: for each keycap-character policy key, `FE0E` is a sanctioned text variation selector for the base character in the pinned data. In a keycap-character spelling, that selector appears before `U+20E3`; it is not an orphaned selector after the keycap mark. Older standardization discussions considered text-selector keycap spellings, even though current emoji qualification defines only the `FE0F 20E3` emoji form for RGI keycap bases.

Inference: **preserving an explicit text selector in keycap context treats it as a local text-presentation request on the base before the enclosing keycap mark.** Converting it to `FE0F` would erase an explicit sanctioned selector state rather than merely repairing malformed text.

### ASCII Non-Keycap Bare Default

Manually reviewed: yes.

Facts: ASCII digits, `#`, and `*` are common source characters, and they also have sanctioned text and emoji variation sequences. They frequently appear as syntax, numeric data, Markdown markers, operators, and other ordinary text rather than as deliberate glyph-presentation requests.

Product assumption: **ASCII selector slots outside keycap context should default to bare text-style spelling.** The strongest reason is to avoid generating `FE0E` in common ASCII source text. Under the canonical policy model, choosing explicit `FE0E` as the text-side canonical spelling would generate invisible presentation controls for bare non-keycap ASCII input. Choosing bare as the canonical text-side spelling avoids that generation, but also normalizes existing non-keycap ASCII `FE0E` to bare.

This is a policy-model tradeoff. Preserving an existing non-keycap ASCII `FE0E` without generating new `FE0E` would require a more source-sensitive policy shape rather than a different default membership in the current canonical policy sets.

### Text-Default Non-Keycap Bare Interpretation

Manually reviewed: yes.

Facts: some non-keycap policy keys are text-default bases outside the ASCII set. Their bare spelling follows Unicode text default, and both `FE0E` and `FE0F` are sanctioned selector states.

Product assumption: **the default policy treats bare text-default input in non-keycap selector slots as text presentation unless another rationale gives that key a stronger emoji-side default.** This is the default membership choice behind putting `text-defaults` in `bare_as_text`. It follows Unicode default presentation for bare source text: for non-ASCII text-default keys that do not prefer bare, the canonical explicit spelling is `FE0E`.

RGI data does not decide this text-vs-emoji default for non-keycap, non-RGI source text. RGI preference only constrains the chosen canonical form when default policy chooses a form with emoji presentation and the RGI spelling is reasonable.

Evidence gap: user intent for bare non-ASCII text-default characters is uncertain. A useful counterargument is ZWJ repair tolerance: if bare text-default components in ZWJ-related input often represent missing `FE0F`, an emoji-side default would avoid introducing `FE0E` and would preserve emoji rendering more often. Evidence that common source files use bare text-default forms primarily as emoji presentation, for example because tools often display emoji while copying or storing the bare text-default form, should move the affected keys out of `bare_as_text` or into another more specific policy default.

### Emoji-Default Bare Policy

Manually reviewed: yes.

Facts: some standalone variation-sequence bases are emoji-default, so their bare spelling already carries emoji presentation by Unicode default.

Inference: **emoji-default bases should use their bare canonical spelling.** For emoji-default bases, a bare canonical spelling matches the Unicode default while avoiding redundant `FE0F`. It is also the RGI spelling for the sequence; adding redundant `FE0F` does not produce the RGI form. For future renderers, fonts, and platforms, honoring the Unicode default is the least-surprising long-term behavior.

Evidence gap: this supports bare canonical spelling for emoji-default bases, but it does not prove every document type prefers Unicode-default compactness over explicit selector spelling.
